# HW
Design a dispatch service that will allow packages to arrive at a dispatch desk and leave via various methods.
## Running tests
1. Clone repo
2. at the root, run pip install requirements.txt
3. type `pytest` in the terminal at the root directory
