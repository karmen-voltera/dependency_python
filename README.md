# HW
Design a dispatch service that will allow packages to arrive at a dispatch desk and leave via various methods.
Detailed question can be found in Question.md
## Running tests
1. Clone repo
2. run `pip install requirements.txt` in the root directory
3. run `python -m pytest` in the root directory
